[project]
name = "callmemaybe"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "pydantic>=2,<3",
  "llm_sdk"
]

[tool.uv.sources]
llm_sdk = { path = "./llm_sdk", editable = true }
